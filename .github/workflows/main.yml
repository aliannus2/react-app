name: GitHub Actions Demo
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
    - name: Run script file
      run: |
         chmod +x ./file/project-fetcher.sh
         ./file/project-fetcher.sh
      shell: bash
